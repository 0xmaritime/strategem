Analyze the following Target System using the Qualitative Systems Dynamics framework.

⸻

DECISION CONTEXT (V2: OPTIONAL)

Target System: {target_system_title}
Decision Question: {decision_question}
Decision Type: {decision_type}
Options Under Consideration: {options}

⸻

PROBLEM CONTEXT MATERIAL

{context}

⸻

YOUR TASK

Analyze target system's internal dynamics. If decision context is provided, analyze how each option is affected differently.

V2 REQUIREMENTS:
- Decision context is OPTIONAL (can run with problem context only)
- If decision options are provided, claims should be option-aware (specify which options they affect)
- If no options provided, produce global claims (apply to entire target system)
- Analysis should not require decision context to function

⸻

REQUIRED OUTPUT STRUCTURE

Provide your analysis in the following JSON format:

{{
  "DecisionQuestion": "{decision_question}",
  "OptionsAnalyzed": {options_list},

  "SystemOverview": "1-2 paragraph narrative describing how the target system operates and its structure",

  "KeyComponents": [
    "component 1",
    "component 2",
    "component 3"
  ],

  "FeedbackLoops": {{
    "Reinforcing": [
      {{
        "Description": "loop 1 description",
        "AffectedOptions": ["Option A", "Option B"],
        "EffectType": "growth_driver | acceleration_mechanism",
        "Assumptions": ["assumption 1", "assumption 2"]
      }}
    ],
    "Balancing": [
      {{
        "Description": "loop 1 description",
        "AffectedOptions": ["Option A"],
        "EffectType": "constraint | stabilizer",
        "Assumptions": ["assumption 1"]
      }}
    ]
  }},

  "Bottlenecks": [
    {{
      "Description": "bottleneck 1",
      "AffectedOptions": ["Option A", "Option B", "Option C"],
      "Severity": "high | medium | low",
      "Unknowns": ["unknown 1", "unknown 2"]
    }}
  ],

  "Fragilities": [
    {{
      "Description": "fragility 1",
      "AffectedOptions": ["Option A"],
      "Severity": "high | medium | low",
      "Cascading": true | false,
      "Assumptions": ["assumption 1"]
    }}
  ],

  "OptionAwareClaims": [
    {{
      "ClaimId": "claim_1",
      "Statement": "Option A's fragility to supply chain disruption is higher than Option B",
      "AffectedOptions": ["Option A", "Option B"],
      "Source": "inference",
      "Confidence": "medium",
      "Framework": "systems_dynamics_v2"
    }}
  ],

  "GlobalClaims": [
    {{
      "ClaimId": "global_claim_1",
      "Statement": "Global claim about the target system that applies regardless of options",
      "Source": "inference",
      "Confidence": "medium",
      "Framework": "systems_dynamics_v2"
    }}
  ],

  "Assumptions": [
    "assumption 1",
    "assumption 2"
  ],

  "Unknowns": [
    {{
      "Statement": "unknown 1",
      "AffectedOptions": ["Option A", "Option B"],
      "Sensitivity": "critical | high | medium | low",
      "EvidenceNeeded": "What evidence would reduce this unknown"
    }}
  ]
}}

⸻

CRITICAL V2 RULES

1. Decision Context is OPTIONAL: Analysis can proceed with problem context only.
2. Option-Aware When Possible: If options are provided, claims should specify affected_options. If not, global claims are acceptable.
3. Flexible Analysis: Every system element may specify which options it affects if relevant.
4. Explicit Non-Goals: Do not make decisions, recommendations, or optimization advice.
5. Framework disagreement is a valid outcome.

⸻

VALIDATION CHECKLIST

Before outputting, verify:
- [ ] Decision question is present if provided (optional if not)
- [ ] Every component, loop, bottleneck, and fragility may specify affected_options if options are present
- [ ] OptionAwareClaims has specific affected_options if options are provided (optional if not)
- [ ] Claims tie system elements to option outcomes if options are relevant
- [ ] No recommendation or ranking is present
- [ ] Output is valid JSON only

⸻

CRITICAL: Output ONLY valid JSON. No markdown, no bold text, no explanations outside the JSON.

Remember: V2 supports optional decision context. If options are provided, claims should be option-aware. If not, global analysis is acceptable.
